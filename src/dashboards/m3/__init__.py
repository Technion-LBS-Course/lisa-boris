"""M3 Dashboard tab renderers.

Each module exposes a ``render(...)`` function for one M3 tab. They are composed by
``src.dashboards.m3_dashboard``. Shared model-rendering logic lives in
``src.dashboards.model_helpers``; heavy ML libraries stay lazy.
"""
